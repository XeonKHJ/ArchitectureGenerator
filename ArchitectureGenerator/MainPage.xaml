<Page
    x:Class="ArchitectureGenerator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArchitectureGenerator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" x:Name="OpenFolderButton" Click="OpenFolderButton_Click" Margin="10">Open Folder</Button>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto">
        <Canvas x:Name="ArchitectureCanvas">
            <Button Margin="5" Visibility="Collapsed">
                <Button.Content>
                    <Grid>
                        <Rectangle >
                        </Rectangle>
                        <TextBlock Text="skdfjsldfsdfsadfsdfasdfsadfs" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
                    </Grid>
                </Button.Content>
            </Button>
        </Canvas>
        </ScrollViewer>
    </Grid>
</Page>
